# V3.3 Safety Notes

- Separate tester entry filenames prevent accidental replacement of root production entry pages.
- No Firebase initialization is present in tester source.
- No production API endpoint is present in tester source.
- Student persistence is namespaced under `dw-v33-tester:` and is limited to harmless tester preferences/draft text.
- Teacher actions are local mock state only.
- The supplied screenshot set remains the visual authority. Production functionality remains the authority for eventual integration, but production code is not modified by this tester package.
