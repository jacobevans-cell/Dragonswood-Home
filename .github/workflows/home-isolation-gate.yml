name: Dragonswood Home isolation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  firebase-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm run home:isolation:audit
